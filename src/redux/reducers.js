const initialState = {
  todos: [],
};

const todoReducer = (state = initialState, action) => {
  switch (
    action.type
    //Write a code as per your need

    // default:
    //   return state;
  ) {
  }
};

export default todoReducer;
